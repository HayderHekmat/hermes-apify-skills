# Integration Notes For `crm_data_enrichment_agent`

## How To Use With Hermes Agent

1. Create a folder named `crm_data_enrichment_agent` in your Hermes Agent skills directory.
2. Place `SKILL.md`, `manifest.json`, `input_schema.json`, `tool_call_contract.json`, `examples.md`, `test_prompts.md`, and `integration_notes.md` in that folder.
3. Restart or refresh the agent skill loader.
4. Test with the prompts in `test_prompts.md` before using the skill for production work.

## Source

- Source URL: https://apify.com/solutionssmart/crm-data-enrichment-agent
- Source type: apify_actor
- Generated by: Hermes Skill Builder for Apify & APIs

## Endpoint Or Operation Summary

- Start run: https://api.apify.com/v2/acts/solutionssmart~crm-data-enrichment-agent/runs
- Run synchronously and return dataset items: https://api.apify.com/v2/acts/solutionssmart~crm-data-enrichment-agent/run-sync-get-dataset-items
- Dataset items: https://api.apify.com/v2/datasets/{defaultDatasetId}/items?format=json&clean=true

## Apify Token And Input Handling

- Store the Apify API token as `APIFY_TOKEN` in Hermes Agent or the host agent's secret store.
- Pass validated user input directly as the JSON body for `https://api.apify.com/v2/acts/solutionssmart~crm-data-enrichment-agent/runs`.
- For quick tests, POST to `https://api.apify.com/v2/acts/solutionssmart~crm-data-enrichment-agent/run-sync-get-dataset-items` and return the dataset items response.
- For larger jobs, POST to `https://api.apify.com/v2/acts/solutionssmart~crm-data-enrichment-agent/runs`, wait for completion, then fetch `https://api.apify.com/v2/datasets/{defaultDatasetId}/items?format=json&clean=true` with the returned `defaultDatasetId`.
- Do not print or store the raw `APIFY_TOKEN` in conversation logs.


## Default Agent Behavior

- Default mode: `not inferred`
- Ask only for these fields during normal use: `emailField`
- Do not expose these fields in normal conversation: `datasetId`
- Use these fields only for expert users or explicit requests: `datasetId`, `maxContacts`, `enrichment`
- Conditional-only fields: none
- Apply `safeDefaults` before the first test run.
- Warn the user before enabling or increasing these fields: `maxContacts`, `enrichment`




## Agent Execution Safety

- Start with `recommendedTestInput` from `tool_call_contract.json`.
- Validate conditional requirements before calling the Actor.
- Avoid high-cost fields unless the user explicitly requests deep scraping, reviews, browser work, or enrichment.
- Use the run-sync endpoint only for small jobs.
- Use the async run endpoint for larger jobs.


## Internal Fields

Internal/debug fields detected: `datasetId`

The agent should not ask the user about these fields by default. Use them only when the user explicitly requests debugging, checkpointing, storage customization, or billing-event testing.


## Known Limitations

- Could not infer conditional requirements from the schema with high confidence.
- The input schema was extracted from Apify Actor metadata. Review conditional requirements described in the Actor README before large runs.
- This package is Hermes-ready but is not an official Hermes integration.

## Adapting For Other Agents

Use `SKILL.md` as the instruction file, `manifest.json` as package metadata, and `input_schema.json` as the validation contract. Other agent systems can rename these files as needed while preserving their contents.
